# Development / test dependencies (NOT installed in the production image).
-r requirements.txt

pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0      # coverage gate in CI (fail-under floor)
pytest-timeout>=2.2.0  # a hung loopback server must fail the job, not eat 6h
httpx>=0.24.0          # FastAPI TestClient (API / middleware tests)
ruff>=0.7.0            # lint gate (see ruff.toml for the rule set)
