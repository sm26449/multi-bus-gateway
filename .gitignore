# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
.eggs/
*.egg-info/
*.egg

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Config files (keep examples) — every RUNTIME-state file the app writes
# under config/ (audit 2026-08-14: a dev tree where the app ran could commit
# live state; only templates/ and *.example.* are tracked)
config/config.yaml
config/config.yaml.*
config/selected_registers.json
config/virtual_meters.yaml
config/rules.yaml
config/calculated_templates.json
config/builder_profiles.json
config/write_leases.json
config/influx_buffer.jsonl
config/devices/
config/device_templates/
config/certs/
*.bak
*.bak-*

# Environment (contains secrets)
.env

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Docker
.docker/

# Test
.pytest_cache/
.coverage
htmlcov/

# Build
dist/
build/

# Node
node_modules/

# Maintainer-local tools and notes (not part of the published project)
tools/capture-screenshots.js
tools/run-backfill.sh
tools/mqtt_capture_schema.py
docs/PUBLISHING.md
docs/ROADMAP.md
docs/vendor/
# Maintainer-local engineering notes and planning
reviews/
perf/
# Internal audit prompts & history — never published (see git history caveat before going public)
.audit/
config/audit.jsonl*
config/events.jsonl
config/snapshots/
config/passkeys.json
config/sessions.json*

# local assistant workspace — never commit
.claude/
