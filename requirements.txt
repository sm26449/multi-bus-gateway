# Multi-Bus Gateway — runtime dependencies

# Modbus
pymodbus==3.6.9
pyserial>=3.5          # Modbus RTU (serial) transport

# MQTT
paho-mqtt>=2.0.0

# InfluxDB (optional)
influxdb-client>=1.38.0

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Configuration
PyYAML>=6.0.1

# Passkeys / WebAuthn (login without passwords)
webauthn>=2.1.0,<3
